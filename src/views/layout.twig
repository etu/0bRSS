<!DOCTYPE html>
<html>
    <head>
        <title>0bRSS</title>

        <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Open+Sans" />
        <link rel="stylesheet" href="{{ baseUrl() }}/assets/css/style.css" />
    </head>
    <body>
        <header>
            0bRSS
        </header>

        {% if not hide_aside %}
            <aside>
                aside
            </aside>
        {% endif %}

        <div id="content"{{ hide_aside ? ' class="fullwidth"' }}>
            {{ block('content') }}
        </div>
    </body>
</html>
